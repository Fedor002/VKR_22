@model VKR_Visik.Classes.Sections

@{
    ViewData["Title"] = "Create";
}

<h1>Форма создания</h1>

<h4>Секции</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="sections_Name" class="control-label">Имя секции</label>
                <input asp-for="sections_Name" class="form-control" />
                <span asp-validation-for="sections_Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
